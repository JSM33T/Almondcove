import { BlogDomDirective } from './blog-dom.directive';

describe('BlogDomDirective', () => {
  it('should create an instance', () => {
    const directive = new BlogDomDirective();
    expect(directive).toBeTruthy();
  });
});
